from setuptools import setup, find_packages

setup(
    name="limiter",
    version="0.0.5",
    packages=find_packages(),
    description="Limiter",
    include_package_data=True,
    zip_safe=True,
    install_requires=[],
)
